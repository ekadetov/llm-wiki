---
description: "LLM Wiki — init, ingest, compile, query, lint, or remove a knowledge base wiki"
argument-hint: "init <name> | ingest <path|url> | compile [<path>] | query <question> | lint | remove <name>"
---

Load and follow the `llm-wiki:wiki` skill. Pass through all user arguments exactly as provided.
